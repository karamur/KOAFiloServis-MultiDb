# Çalışma Devir Notu — Puantaj & Fatura Geliştirme Paketi

> Son commit: `a81dd85` (main / origin'e push edildi)
> Build durumu: ✅ 0 warning / 0 error
> Hedef: .NET 10 + Blazor (KOAFiloServis.Web)

---

## ✅ 21 Kasım sabah seansında tamamlananlar

1. **Arac.Plaka EF taraması** — tüm proje tarandı; kalan 11 kullanım Razor render / toast / JS confirm / Excel hücresi tarafında, EF query içinde yok. Düzeltme gerekmedi.
2. **Faturasız uyarı şeridi** — OperasyonelHakedis listesinde Onaylandı + faturalanmamış kayıt sayısı/tutarı banner olarak gösteriliyor; "Faturalanmamışları Göster" butonu otomatik filtre uyguluyor (`filtreFatura = "Yok"`, `filtreDurum = "Onaylandi"`).
3. **Hakediş detayında fatura kalemleri özeti** — `IFaturaService` enjekte edildi; `DetayAc` faturayı ve `FaturaKalemleri`'ni `faturaCache`'e yüklüyor. Detay panelinin başında fatura no, tarih, vade, durum, genel toplam, kalan + kalem listesi (sıra/açıklama/miktar/birim/birim ₺/KDV/tutar) gösteriliyor.
4. **Vade rengi badge** — `Fatura No` kolonundaki linkin yanında: `Kapandı` (kalan ≤ 0), `Geçmiş Ng` (kırmızı), `Ng` ≤ 7 gün (sarı), aksi gri. `VadeDurum(Fatura)` helper'ı eklendi.


### Hakediş listesi UX (OperasyonelHakedis.razor)
- ✅ **Fatura No kolonu** eklendi. `FaturaId` varsa `Fatura.FaturaNo` tıklanabilir link olarak `/faturalar/{id}` açıyor; yoksa "-".
- ✅ **Faturalanmış / Faturalanmamış filtresi** eklendi (`filtreFatura` state, `OnFaturaFiltreChanged`).
- ✅ Yeni `FaturaNo` kolonu için **sıralama** desteği (`SiralamaDegistir("FaturaNo")`).
- ✅ `FiltreyiTemizle` artık fatura filtresini de resetliyor; detay genişletme satırının `colspan` değeri 11'e çekildi.
- ✅ **Excel export** güncellendi: başlığa fatura filtre etiketi yansıyor ("Faturalanmış / Faturalanmamış") ve dışa aktarımda `Fatura No` kolonu mevcut. Toplam satırı 11. kolona genişletildi.

### Servis tarafı (OperasyonelHakedisService.cs)
- ✅ `GetHakedislerAsync` artık `Include(h => h.Fatura)` ile fatura navigation'ını yüklüyor (UI'daki `h.Fatura?.FaturaNo` erişimi için gerekli).

### Hata düzeltme — FiloGunlukPuantajPage.razor
- ✅ `LoadAsync` içinde EF Core "Translation failed" hatası düzeltildi.
  - **Sorun:** `OrderBy(p => p.Arac!.Plaka)` → `Arac.Plaka` `[NotMapped]` alias.
  - **Çözüm:** `OrderBy(p => p.Arac!.AktifPlaka)` (mapped kolon).

### Önceki paket içinde (referans)
- ✅ `Raporlar/AracMaliyetGelir.razor` (araç bazlı maliyet vs gelir) + NavMenu link.
- ✅ `ServisOperasyon/PuantajDetay.razor` üzerinden Giden/Gelen fatura taslağı oluşturma + `/faturalar/{id}` yönlendirme.
- ✅ `OperasyonelHakedis.razor` tekil "Faturaya Dönüştür" sonrası fatura detayına yönlendirme + "Faturayı Aç" butonu.
- ✅ Toplu faturalama sonuç tablosunda fatura linki (`TopluIslemSatir.FaturaId/FaturaNo`).
- ✅ `KiralikPlakaForm.razor` CS8629 ve `Services/OpenRouterService.cs` CA2024 warning'leri temizlendi.
- ✅ Uyarı/hata metinleri standardize edildi.

---

## 2) Yarın devam edilecek işler (öncelik sırası)

### 🔥 Yüksek öncelik
1. **`Arac.Plaka` alias kaynaklı diğer EF sorgularını tara.**
   - Aynı pattern başka rapor/listede de varsa ("Translation failed") aynı hatayı verir.
   - Tarama komutu (PowerShell):
     ```powershell
     Select-String -Path .\KOAFiloServis.Web\**\*.cs,.\KOAFiloServis.Web\**\*.razor -Pattern "\.Arac[!?]?\.Plaka" -SimpleMatch:$false
     ```
   - Eşleşenleri `OrderBy/Where/Select` içinde değilse bırak; EF query içinde olanları `AktifPlaka` ile değiştir.

2. **Faturasız hakediş için liste üstünde uyarı şeridi** (OperasyonelHakedis.razor):
   - Onaylandı durumda olup `FaturaId == null` olanların sayısı banner olarak gösterilsin, "Faturalanmamışları Gör" butonu `filtreFatura = "Yok"` set edip `LoadAsync` çağırsın.

3. **Hakediş detayında fatura kalemleri özeti** (PuantajDetay / OperasyonelHakedis detay paneli):
   - `FaturaId` varsa fatura kalemleri (FaturaKalem) mini özet (kalem adı, miktar, tutar) gösterilsin.

### 🟡 Orta öncelik
4. **Vade rengi** — fatura linkinin yanında vade durumuna göre badge:
   - `VadeTarihi < bugün && KalanTutar > 0` → kırmızı "Geçmiş",
   - 7 gün içinde → sarı,
   - aksi → varsayılan.

5. **Toplu faturalama sonuç modali** — başarısızları açılır panele topla, kopyalanabilir hata listesi.

6. **Kurum bazlı hakediş özet kartı** — listede aktif `Kurum` filtresi varsa o kurumun aylık trendi (mini sparkline).

### 🟢 Düşük öncelik / iyileştirme
7. `OperasyonelHakedis.razor` 1500+ satır oldu — code-behind (`.razor.cs`) veya kısmi component'lere (filtre / liste / modal) bölmeyi planla.
8. Hakediş PDF'inde `FaturaNo` ve fatura tarihini header'a ekle.
9. `Fatura` tarafında ters bağ (`Fatura.HakedisId` veya `ICollection<Hakedis>`) tanımlı mı kontrol et; UI'da fatura → hakediş geri linki için gerekli.

---

## 3) Bilinen kısıtlar / dikkat notları

- `Hakedis` üzerinde alan adları: `Yil`, `Ay`, `FaturaId`, `Fatura` (navigation), `CreatedAt`. **`Donem` / `OlusturmaTarihi` yok.**
- `Fatura` üzerinde doğru alan: **`FaturaNo`** (FaturaNumarasi DEĞİL).
- `Firma` üzerinde doğru alan: **`FirmaAdi`** (Unvan DEĞİL).
- `Arac.Plaka` `[NotMapped]` aliastır. EF query içinde her zaman `AktifPlaka` kullan.
- `IOperasyonelHakedisService.GetHakedislerAsync(yil, ay, tip?, referansId?, durum?)` imzası kullanımda; başka bir `GetAllAsync` yok.
- Kullanıcı tercihi: **sohbet Türkçe** (`copilot-instructions.md`).

---

## 4) Aktif dosyalar (yarın açılacak referanslar)

- `KOAFiloServis.Web/Components/Pages/Hakedis/OperasyonelHakedis.razor` (ana ekran)
- `KOAFiloServis.Web/Services/OperasyonelHakedisService.cs` (Include + servis akışı)
- `KOAFiloServis.Web/Services/Interfaces/IOperasyonelHakedisService.cs`
- `KOAFiloServis.Web/Components/Pages/Filo/FiloGunlukPuantajPage.razor` (son hata düzeltildi)
- `KOAFiloServis.Web/Components/Pages/ServisOperasyon/PuantajDetay.razor` (fatura taslağı pattern)
- `KOAFiloServis.Web/Components/Pages/Raporlar/AracMaliyetGelir.razor` (rapor stil referansı)
- `KOAFiloServis.Shared/Entities/Hakedis.cs`
- `KOAFiloServis.Shared/Entities/Fatura.cs`
- `KOAFiloServis.Shared/Entities/Arac.cs` (Plaka NotMapped, AktifPlaka mapped)

---

## 5) Hızlı başlangıç (yarın)

```powershell
cd "C:\Users\muratk\Desktop\d yedek\calisma\Claude-Code\KOAFiloServis"
git pull
dotnet build
```

Sonra Copilot Chat'e: *"HANDOFF-2025-11-21.md'deki 1. öncelikten devam edelim"* deyince doğrudan tarama + düzeltme akışına girilebilir.
